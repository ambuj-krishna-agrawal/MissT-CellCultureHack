# Standalone scripts using ur_rtde (SDU Robotics)
# Install: pip install -r requirements.txt
ur-rtde>=1.5.0

# Perception (ArUco table + 3D object positions)
opencv-contrib-python>=4.6.0
PyYAML>=5.0
numpy>=1.20.0

# Guaranteed path planner (joint-space, singularity/limit-safe); optional for automate.py
ikpy>=3.3.0
